# Local DB Container（docker-compose.yml の db サービス）への接続情報。
# 使い方: cp .env.example .env
#
# 既定値のまま `docker compose up -d db` で動作する。
# 外部のDBへ向ける場合はここを書き換える（コードの変更は不要）。
DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=root
DB_PASSWORD=root
DB_NAME=sandbox
